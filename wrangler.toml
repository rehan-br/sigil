name = "sigil"
compatibility_date = "2025-04-01"
pages_build_output_dir = "dist"

# KV binding for SIGIL_KV is configured in the Cloudflare Pages dashboard
# (Settings → Functions → KV namespace bindings) rather than here, so the
# namespace ID isn't checked into source. The runtime binding name is
# `SIGIL_KV` — match that in the dashboard.
